TODO before 0.04
----------------
* anti-dos (many connections)
* proper logging


TODO before 0.05
----------------
* proper wtmp update at logout


TODO before 1.00
---------------
* Listen option
* Provide grsec RBAC config
* port to Solaris
** cfmakeraw - done?
** forkpty - done?
** tcpmd5 - done?
** clearenv - skeleton
** setres[ug]id - done?
** logwtmp (whole utmp handling)
** basename - done?
** daemon - done?
* port to OpenBSD
** wordexp
** tcpmd5
* Manpage - done
* other documentation (README, INSTALL, HACKING)
* kill connection when cert expires
* document the code
* get someone to audit the code


TODO after 1.00
-------------
* pick a proper port number
* HUP re-reads config file and re-overrides using the old cmdline
* Client has send-local-file-as-typed support.
* xmodem file send/recv
* EGADS / EGD support (RAND_egd())
* after auth, reset TCP signatures to something secret so that the
  connection is unresettable
* set QoS
* TCP MD5 should be on the listening socket.
* pipe:ing.  tlssh host.com echo foo > foo 2>/dev/null
* support for online CRL check (OCSP)


Notes
-----
def seed_random():
  seed random from /dev/random (RAND_load_file)
  add some random from /var/run/tlssh.random (RAND_add)
  write a new /var/run/tlssh.random (RAND_write_file)
